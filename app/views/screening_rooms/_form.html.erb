<%= form_for(@screening_room, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @screening_room.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@screening_room.errors.count, "error") %> prohibited this screening_room from being saved:</h4>

      <ul>
      <% @screening_room.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :theater_number, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.number_field :theater_number, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :seat_quantity, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.number_field :seat_quantity, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :movie_theater_id, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.number_field :movie_theater_id, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
